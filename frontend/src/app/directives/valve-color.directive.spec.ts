import { ButtonColorDirective } from './valve-color.directive';

describe('ButtonColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonColorDirective();
    expect(directive).toBeTruthy();
  });
});
