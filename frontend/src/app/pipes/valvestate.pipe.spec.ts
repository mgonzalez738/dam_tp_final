import { ValveStatePipe } from './valvestate.pipe';

describe('ValveStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ValveStatePipe();
    expect(pipe).toBeTruthy();
  });
});
